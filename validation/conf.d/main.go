package main

func main() {

	// cfg := make(map[string]validation.Context)
	// filePath, err := filepath.Abs("./validation.toml")
	// if err != nil {
	// 	panic(err)
	// }
	// fmt.Printf("parse toml file once. filePath: %s\n", filePath)
	// if _, err := toml.DecodeFile(filePath, &cfg); err != nil {
	// 	panic(err)
	// }

	// for k, v := range cfg {

	// 	for k1, v1 := range v {
	// 		fmt.Println(k + "." + k1)
	// 		for _, rule := range v1 {
	// 			fmt.Println(rule)
	// 		}

	// 	}
	// }

}
